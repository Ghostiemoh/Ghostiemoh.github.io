## 2025-05-15 - [LCP Image Optimization]
**Learning:** Found LCP image (`profile.JPG`) configured with `loading="lazy"`, which delays LCP.
**Action:** For above-the-fold images, always ensure `loading="lazy"` is removed and `fetchpriority="high"` is considered.
